{
  "name": "secondary-disaster-management-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
