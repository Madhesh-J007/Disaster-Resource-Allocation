project:
  name: Secondary Disaster Chain Reaction ML
  version: 0.1.0

paths:
  raw_data: datasets/raw
  interim_data: datasets/interim
  processed_data: datasets/processed
  models: models/saved_models
  outputs: outputs

params:
  random_seed: 42
  test_size: 0.2
  val_size: 0.1
